---
date: 2026-04-28
session: auto-edge creation + PageRank — implementation, port, SI extraction
agent: Claude Opus 4.6 (200K context, extended thinking) via Claude Code
prior-summary: null
plan: docs/plans/2026-04-28-001-feat-auto-edge-creation-pagerank-port-plan.md (thinking-mcp-pro)
---

## Headline

Shipped auto-edge creation + PageRank in thinking-mcp@0.2.0 (public, npm published), ported to thinking-mcp-pro, and extracted a standalone database-agnostic auto-edge module into Software Incubator as a catalog entry for reuse in davidbase-mining.

## Completed work

| Item | Repo | Files | Status |
|------|------|-------|--------|
| Auto-edge creation (2-stage) | thinking-mcp | `src/tools.ts`, `src/cosine.ts`, `src/pagerank.ts`, `src/extract.ts`, `src/types.ts`, `src/db.ts`, `src/embed.ts` | Merged to main, published @multimail/thinking-mcp@0.2.0 |
| PageRank as 5th RRF signal | thinking-mcp | `src/tools.ts`, `src/pagerank.ts` | Merged to main |
| SQL parameterization | thinking-mcp | `src/tools.ts`, `src/db.ts` | Merged to main |
| README rewrite (Kahneman framing) | thinking-mcp | `README.md` | Merged to main |
| npm publish | thinking-mcp | `package.json` v0.2.0 | Published to npm |
| Port to thinking-mcp-pro | thinking-mcp-pro | Same 8 files + preserved vault cross-links | Merged to feat/portable-vault-cognitive-mcp |
| README rewrite (internal, frank) | thinking-mcp-pro | `README.md` | Merged to feat/portable-vault-cognitive-mcp |
| SI catalog extraction | Software Incubator | `catalog/ts/graph/auto-edge/{auto-edge.ts,auto-edge.test.ts,auto-edge.llm.md}` | PR #20 open |

## Delegation ledger

| What | Delegate | Tests written by | Supervisor reviewed | Verdict |
|------|----------|------------------|---------------------|---------|
| Plan review (thinking-mcp-pro) | adversary agent | NON-TDD — review task, not implementation | Yes, 2026-04-28 | Found 2 critical gaps (addCrossLink in two-pass, vault.ts SQL injection), 2 warnings. All addressed in plan update. |
| Implementation review (thinking-mcp-pro) | adversary agent | NON-TDD — review task, not implementation | Yes, 2026-04-28 | GO verdict. All 6 checks passed. Warning about remaining db.exec template literals (system-generated UUIDs, not exploitable). |
| Earlier session (pre-compaction): codex review of thinking-mcp PR | codex (via /delegate) | NON-TDD — review task | Yes, 2026-04-28 | Multiple rounds on Sharp Directives 1 and 3. Found 3 implementation bugs (PageRank filter, two-pass flow, cache invalidation). All fixed. |

## Code quality assessment

- **TypeScript strict**: Clean compile in both thinking-mcp and thinking-mcp-pro
- **SQL injection**: Keyword search paths parameterized (what_do_i_think, searchNodes). Remaining db.exec template literals use system-generated UUIDs — tech debt, not exploitable.
- **Error isolation**: autoCreateEdges wraps both stages in try/catch — edge failures never block capture.
- **Known out-of-scope**: vault.ts SQL injection (toolSearchVault, toolSearchAll) — deferred, separate concern.

## Main branch state

**thinking-mcp (public)**:
- `main` HEAD: `1f378ee` — `docs: rewrite README with Kahneman framing and updated feature descriptions`
- npm: `@multimail/thinking-mcp@0.2.0` published
- No open PRs, no stale branches

**thinking-mcp-pro (private)**:
- `feat/portable-vault-cognitive-mcp` HEAD: `5a0b0b8` — `docs: rewrite README with edge creation, PageRank, and full tool reference`
- `feat/auto-edge-pagerank` merged into above, can be deleted
- No open PRs

**Software Incubator**:
- `main` HEAD: `2be7f12`
- PR #20 open: `feat/auto-edge-catalog-entry` → `main`
- Branch `feat/auto-edge-catalog-entry` HEAD: `d53d2ec`

## Pending / next session

1. **Port auto-edge + PageRank to davidbase-mining** — the "momma repo" running on CF Workers + D1. Key differences:
   - D1 (not sql.js) — need D1-specific `AutoEdgeCallbacks` implementation
   - Worker structure: `davidbase_mining/worker/src/` with only `auth.ts` + `index.ts` currently
   - Will vendor-copy from SI catalog or import the standalone module
   - Need to understand the full schema at `davidbase_mining/worker/schema.sql`

2. **Merge SI PR #20** — catalog entry for auto-edge

3. **Fix get_framing in davidbase-mining** — user noted "davidbase-mining MCP get_framing directives haven't been firing"

4. **Migrate thinking-mcp and thinking-mcp-pro to import from SI catalog** — currently inline copies. Low priority (working fine as-is).

## Decisions still open

- **davidbase-mining: vendor-copy or npm import?** — SI catalog items are designed for vendor-copy (the deposit script stamps vendor headers). But davidbase-mining is CF Workers — might prefer a shared package. Decide next session.
- **Cross-repo edge types**: Should vault_edges and cognitive edges share a PageRank? Currently separate graphs. Potential compounding asset if unified.

## Worth capturing as feedback memories

- **Two-pass capture pattern is critical for multi-pattern extraction** — the codex review caught that edges created during node insertion can't find nodes that haven't been inserted yet. This pattern (insert all, then edge all) should be in the SI sidecar doc. ✅ Done.
- **addCrossLink preservation is the #1 risk when porting** — the adversary review correctly identified that the reference implementation (thinking-mcp) has no vault integration, so naive copy-paste drops cross-links. Document this in any future port plan.
- **Codex review Sharp Directive gates are expensive but effective** — 5 rounds on SD1 (consumer trace), 3 rounds on SD3 (description accuracy) before the thinking-mcp PR could merge. Each round caught a real issue.

## Stumbling blocks encountered

1. **npm publish 401** — token expired. Required user intervention to create new token. No way to detect this proactively.
2. **Codex gate loops** — the pre-plan-commit hook blocked PR merge repeatedly on Sharp Directives. The consumer trace comment block and capture description accuracy issues were real but the iteration cost was high (8 rounds total).
3. **Context compaction** — session was compacted mid-plan. Recovered cleanly from the summary but lost some file-read cache context.

## Recommendations for next session

1. **Read davidbase-mining schema.sql first** — understand the D1 schema before planning the port. It may differ from sql.js thinking-mcp.
2. **Fix get_framing in davidbase-mining before porting auto-edge** — user explicitly mentioned this is broken. Quick win, builds momentum.
3. **Use the SI auto-edge module directly** — don't inline-copy again. Wire up D1-specific callbacks against the generic interface.
4. **Plan the vault_edges → cognitive edges PageRank unification** — potential high-leverage compounding asset.

## Constraint-removal note

The constraint this session optimized around was "port identical code across three repos." If that constraint went away, the compounding asset would be a shared npm package (`@multimail/cognitive-graph-core`) containing auto-edge, PageRank, cosine, RRF fusion, and the type system — imported by all three repos instead of vendor-copied. The SI catalog entry is a step toward this but stops short of a published package. The next leverage point is unifying vault_edges and cognitive edges into a single PageRank graph, which would make the vault layer a first-class input to structural importance ranking.

## Vault MCP write

Skipped — vault-svc MCP unavailable (CF_ACCESS_CLIENT_ID / CF_ACCESS_CLIENT_SECRET not in env).
